# match_predict — project rules (index)

Claude Code auto-loads this file. The detailed rules live in `rules/` and are imported
below so they are actually enforced (a bare `rules/` directory is not auto-loaded on its
own). The root `CLAUDE.md` holds the operator's original milestone brief; these add the
conventions the codebase enforces.

@rules/00-core.md
@rules/10-no-leakage.md
@rules/20-betting.md
@rules/30-conventions.md
@rules/40-python-style.md
@rules/50-comments.md

See also: the skills in `skills/` (football-backtest, football-predict-check)
and the `match-predict-dev` project agent in `agents/`.
